name: Manual deploy

on:
  workflow_dispatch:
    inputs:
      target:
        type: choice
        required: true
        default: ecr
        options:
        - ecr

jobs:
  deploy:
    uses: ./.github/workflows/_deploy.yml
    secrets: inherit
    with:
      target: ${{ inputs.target }}
